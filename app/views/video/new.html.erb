<%= form_with(model: @video, url: video_index_path, method: :post, html: { multipart: true }) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.label :video %>
  <%= form.file_field :video %>

  <%= form.submit 'Upload Video' %>
<% end %>
